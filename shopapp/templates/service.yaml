apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopapp.fullname" . }}
  labels:
    {{- include "shopapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopapp.selectorLabels" . | nindent 4 }}
