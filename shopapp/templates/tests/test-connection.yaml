apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopapp.fullname" . }}-test-connection"
  labels:
    {{- include "shopapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
